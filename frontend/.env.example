# Backend API adresi (opsiyonel).
# Tanımlıysa frontend içeriği bu API'den çeker (ürün, kategori, teknoloji, marka, SSS, ayarlar...).
# Boşsa (ör. GitHub Pages statik dağıtımı) bundle içindeki statik veriler kullanılır.
# Dev'de boş bırakılabilir; Vite proxy /api'yi http://localhost:4000'e yönlendirir.
VITE_API_URL=
